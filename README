Installation
============

$ carton install
$ cp mojo_golf.conf.sample mojo_golf.conf
$ $EDITOR mojo_golf.conf
$ sqlite3 db.db < etc/schema.sql
$ carton exec script/rosedb_generate

Starting up
===========

$ carton exec morbo script/mojo_golf
